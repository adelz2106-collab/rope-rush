<!DOCTYPE html> <html lang="en"> <head> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Mobile scaling --> <meta charset="UTF-8"> <title>Rope Rush: Untangle in Time!</title> <style> body { font-family: Arial, sans-serif; text-align: center; background: #f0f0f0; margin:0; } #gameArea, #leaderboard { display: none; margin-top: 20px; } .knot { display: inline-block; width: 80px; height: 80px; /* bigger for touch */ background: #ccc; margin: 10px; line-height: 80px; cursor: pointer; border-radius: 50%; font-weight: bold; font-size: 20px; user-select: none; } .untied { background: #8f8; } #timer { font-size: 22px; margin-top: 10px; } #attempts { font-size: 18px; margin-top: 10px; } button { font-size: 18px; padding: 12px 20px; margin-top: 15px; border-radius: 8px; border: none; background: #4CAF50; color: white; cursor: pointer; } button:active { background: #45a049; } .confetti { position: fixed; width: 12px; height: 12px; animation: fall 3s linear forwards; } @keyframes fall { to { transform: translateY(100vh) rotate(360deg); opacity: 0; } } #victoryBanner { display: none; position: fixed; top: 15%; left: 50%; transform: translateX(-50%); background: white; border: 3px solid #444; padding: 10px; text-align: center; box-shadow: 0 0 20px rgba(0,0,0,0.5); animation: popUp 1s ease forwards; width: 90%; /* fit mobile */ max-width: 500px; } #victoryBanner img { width: 100%; border-radius: 10px; } @keyframes popUp { from { transform: translateX(-50%) scale(0.5); opacity: 0; } to { transform: translateX(-50%) scale(1); opacity: 1; } } </style> </head> <body> <h1>🪢 Rope Rush: Untangle in Time!</h1> <div id="startArea"> <p>Enter your name to begin:</p> <input type="text" id="playerName" placeholder="Your name" style="font-size:18px; padding:8px; width:80%; max-width:300px;"> <br> <button onclick="startGame()">Start Game</button> </div> <div id="gameArea"> <div id="timer">Time: 30</div> <div id="attempts">Attempts left: 3</div> <div id="knotsContainer"></div> </div> <div id="leaderboard"> <h2>🏆 Leaderboard</h2> <ul id="scoresList"></ul> <button onclick="restartGame()">Play Again</button> </div> <!-- Victory Banner with Image --> <div id="victoryBanner"> <img src="save-the-date.jpeg" alt="Save the Date"> </div> <!-- Sounds --> <audio id="weddingBells" src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3"></audio> <audio id="buzzer" src="https://www.soundjay.com/button/beep-07.wav"></audio> <script> let playerName = ""; let timeLeft = 30; let timerInterval; let knots = []; let untiedCount = 0; let leaderboard = []; let attemptsLeft = 3; function startGame() { playerName = document.getElementById("playerName").value.trim(); if (!playerName) return alert("Please enter your name!"); if (attemptsLeft <= 0) { alert("No attempts left!"); return; } document.getElementById("startArea").style.display = "none"; document.getElementById("gameArea").style.display = "block"; generateKnots(5); startTimer(); document.getElementById("attempts").textContent = `Attempts left: ${attemptsLeft}`; } function generateKnots(count) { const container = document.getElementById("knotsContainer"); container.innerHTML = ""; knots = []; untiedCount = 0; for (let i = 0; i < count; i++) { const knot = document.createElement("div"); knot.className = "knot"; knot.textContent = i + 1; knot.onclick = () => untieKnot(knot); container.appendChild(knot); knots.push(knot); } } function untieKnot(knot) { if (!knot.classList.contains("untied")) { knot.classList.add("untied"); untiedCount++; if (untiedCount === knots.length) endGame(true); } } function startTimer() { timeLeft = 30; document.getElementById("timer").textContent = `Time: ${timeLeft}`; timerInterval = setInterval(() => { timeLeft--; document.getElementById("timer").textContent = `Time: ${timeLeft}`; if (timeLeft <= 0) endGame(false); }, 1000); } function endGame(won) { clearInterval(timerInterval); document.getElementById("gameArea").style.display = "none"; document.getElementById("leaderboard").style.display = "block"; if (won) { document.getElementById("weddingBells").play(); launchConfetti(); document.getElementById("victoryBanner").style.display = "block"; } else { document.getElementById("buzzer").play(); attemptsLeft--; } leaderboard.push({ name: playerName, score: untiedCount }); leaderboard.sort((a, b) => b.score - a.score); document.getElementById("scoresList").innerHTML = leaderboard.map(entry => `<li>${entry.name}: ${entry.score} knots</li>`).join(""); } function restartGame() { if (attemptsLeft > 0) { document.getElementById("leaderboard").style.display = "none"; document.getElementById("victoryBanner").style.display = "none"; document.getElementById("startArea").style.display = "block"; } else { alert("Game over! No attempts left."); } } function launchConfetti() { for (let i = 0; i < 80; i++) { const confetti = document.createElement("div"); confetti.className = "confetti"; confetti.style.left = Math.random() * window.innerWidth + "px"; confetti.style.backgroundColor = randomColor(); document.body.appendChild(confetti); setTimeout(() => confetti.remove(), 3000); } } function randomColor() { const colors = ["red", "blue", "yellow", "green", "purple", "orange", "pink"]; return colors[Math.floor(Math.random() * colors.length)]; } </script> </body> </html><!DOCTYPE html> <html lang="en"> <head> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Mobile scaling --> <meta charset="UTF-8"> <title>Rope Rush: Untangle in Time!</title> <style> body { font-family: Arial, sans-serif; text-align: center; background: #f0f0f0; margin:0; } #gameArea, #leaderboard { display: none; margin-top: 20px; } .knot { display: inline-block; width: 80px; height: 80px; /* bigger for touch */ background: #ccc; margin: 10px; line-height: 80px; cursor: pointer; border-radius: 50%; font-weight: bold; font-size: 20px; user-select: none; } .untied { background: #8f8; } #timer { font-size: 22px; margin-top: 10px; } #attempts { font-size: 18px; margin-top: 10px; } button { font-size: 18px; padding: 12px 20px; margin-top: 15px; border-radius: 8px; border: none; background: #4CAF50; color: white; cursor: pointer; } button:active { background: #45a049; } .confetti { position: fixed; width: 12px; height: 12px; animation: fall 3s linear forwards; } @keyframes fall { to { transform: translateY(100vh) rotate(360deg); opacity: 0; } } #victoryBanner { display: none; position: fixed; top: 15%; left: 50%; transform: translateX(-50%); background: white; border: 3px solid #444; padding: 10px; text-align: center; box-shadow: 0 0 20px rgba(0,0,0,0.5); animation: popUp 1s ease forwards; width: 90%; /* fit mobile */ max-width: 500px; } #victoryBanner img { width: 100%; border-radius: 10px; } @keyframes popUp { from { transform: translateX(-50%) scale(0.5); opacity: 0; } to { transform: translateX(-50%) scale(1); opacity: 1; } } </style> </head> <body> <h1>🪢 Rope Rush: Untangle in Time!</h1> <div id="startArea"> <p>Enter your name to begin:</p> <input type="text" id="playerName" placeholder="Your name" style="font-size:18px; padding:8px; width:80%; max-width:300px;"> <br> <button onclick="startGame()">Start Game</button> </div> <div id="gameArea"> <div id="timer">Time: 30</div> <div id="attempts">Attempts left: 3</div> <div id="knotsContainer"></div> </div> <div id="leaderboard"> <h2>🏆 Leaderboard</h2> <ul id="scoresList"></ul> <button onclick="restartGame()">Play Again</button> </div> <!-- Victory Banner with Image --> <div id="victoryBanner"> <img src="save-the-date.jpeg" alt="Save the Date"> </div> <!-- Sounds --> <audio id="weddingBells" src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3"></audio> <audio id="buzzer" src="https://www.soundjay.com/button/beep-07.wav"></audio> <script> let playerName = ""; let timeLeft = 30; let timerInterval; let knots = []; let untiedCount = 0; let leaderboard = []; let attemptsLeft = 3; function startGame() { playerName = document.getElementById("playerName").value.trim(); if (!playerName) return alert("Please enter your name!"); if (attemptsLeft <= 0) { alert("No attempts left!"); return; } document.getElementById("startArea").style.display = "none"; document.getElementById("gameArea").style.display = "block"; generateKnots(5); startTimer(); document.getElementById("attempts").textContent = `Attempts left: ${attemptsLeft}`; } function generateKnots(count) { const container = document.getElementById("knotsContainer"); container.innerHTML = ""; knots = []; untiedCount = 0; for (let i = 0; i < count; i++) { const knot = document.createElement("div"); knot.className = "knot"; knot.textContent = i + 1; knot.onclick = () => untieKnot(knot); container.appendChild(knot); knots.push(knot); } } function untieKnot(knot) { if (!knot.classList.contains("untied")) { knot.classList.add("untied"); untiedCount++; if (untiedCount === knots.length) endGame(true); } } function startTimer() { timeLeft = 30; document.getElementById("timer").textContent = `Time: ${timeLeft}`; timerInterval = setInterval(() => { timeLeft--; document.getElementById("timer").textContent = `Time: ${timeLeft}`; if (timeLeft <= 0) endGame(false); }, 1000); } function endGame(won) { clearInterval(timerInterval); document.getElementById("gameArea").style.display = "none"; document.getElementById("leaderboard").style.display = "block"; if (won) { document.getElementById("weddingBells").play(); launchConfetti(); document.getElementById("victoryBanner").style.display = "block"; } else { document.getElementById("buzzer").play(); attemptsLeft--; } leaderboard.push({ name: playerName, score: untiedCount }); leaderboard.sort((a, b) => b.score - a.score); document.getElementById("scoresList").innerHTML = leaderboard.map(entry => `<li>${entry.name}: ${entry.score} knots</li>`).join(""); } function restartGame() { if (attemptsLeft > 0) { document.getElementById("leaderboard").style.display = "none"; document.getElementById("victoryBanner").style.display = "none"; document.getElementById("startArea").style.display = "block"; } else { alert("Game over! No attempts left."); } } function launchConfetti() { for (let i = 0; i < 80; i++) { const confetti = document.createElement("div"); confetti.className = "confetti"; confetti.style.left = Math.random() * window.innerWidth + "px"; confetti.style.backgroundColor = randomColor(); document.body.appendChild(confetti); setTimeout(() => confetti.remove(), 3000); } } function randomColor() { const colors = ["red", "blue", "yellow", "green", "purple", "orange", "pink"]; return colors[Math.floor(Math.random() * colors.length)]; } </script> </body> </html>
